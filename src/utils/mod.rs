use super::*;

// trait Clamp
// where
//     Self: Sized + PartialOrd,
// {
//     fn clamp(self, range: std::ops::Range<Self>) -> Self {
//         if self < range.start {
//             range.start
//         } else if self > range.end {
//             range.end
//         } else {
//             self
//         }
//     }
// }

// impl<T> Clamp for T where T: PartialOrd {}
